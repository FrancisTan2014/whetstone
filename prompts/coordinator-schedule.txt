Run one whetstone coordinator tick.

The coordinator's scheduling decision is deterministic and is implemented in code, not
re-reasoned here. Your only job each tick is to run that script and report its result.

Do exactly this and nothing else:

1. Run: scripts\coordinator-tick.cmd
2. Report the final `coordinator-tick: <result>` line it prints.

Do not sync GitHub status, inspect labels, choose a developer or reviewer, edit
.agent-status.local.json, or make any scheduling decision yourself. The script performs
lock cleanup, remote status sync, the full decision tree, and invokes at most one one-shot
worker (developer or reviewer). All of that logic lives in:

- scripts\coordinator-tick.mjs  — the deterministic tick (pure `decide()` + side effects)
- scripts\coordinator-tick.test.mjs  — fixture tests covering every decision branch

If the script exits non-zero, report that; do not attempt to do its work manually.
